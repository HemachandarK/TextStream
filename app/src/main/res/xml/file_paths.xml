<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Allow sharing files in app's external files directory -->
    <external-files-path
        name="encrypted_files"
        path="." />

    <!-- Allow sharing files in internal app files/shared_enc/ -->
    <files-path
        name="shared_enc"
        path="shared_enc/" />

</paths>
